extends ./layout/index

block contenido
    div(class="py-10 text-center")
        h1(class="text-6xl my-10 font-extrabold text-fuchsia-600") 404
        h2(class="text-2xl font-bold text-gray-800") Página No Encontrada

        p(class="text-gray-600 mt-5") Lo sentimos, la página que estás buscando no existe o fue movida.

        //- --- Contenedor para los botones de acción (AHORA ES CONDICIONAL) ---
        div(class="mt-8 flex justify-center")
            if usuario
                // Si el usuario SÍ ha iniciado sesión
                a(href="/mis-actividades" class="bg-fuchsia-600 hover:bg-fuchsia-700 text-white font-bold py-3 px-8 rounded-md transition-colors") Volver al Inicio
            else
                // Si el usuario NO ha iniciado sesión
                a(href="/DamarisSpa" class="bg-gradient-to-r from-fuchsia-500 to-purple-600 hover:from-fuchsia-600 hover:to-purple-700 text-white font-bold py-3 px-8 rounded-md transition-all") Iniciar Sesión