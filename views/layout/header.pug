//- Unimos todo en un solo bloque de header para mejor semántica y control
header(class="bg-fuchsia-500 shadow-lg")
    div(class="container mx-auto px-4")
        div(class="flex justify-between items-center py-4")
            a(href="/mis-actividades" class="text-white text-4xl font-light tracking-wide font-serif")
                span(class="font-extrabold text-white") Damaris 
                span(class="ml-2 italic") Spa
            
            // Botón de Logout estilizado para coincidir con el del layout/app que te propuse
            form(method="POST", action="/auth/logout")
                input(type="hidden" name="_csrf" value=csrfToken)
                button(type="submit" title="Cerrar Sesión" class=" cursor-pointer flex items-center justify-center bg-white/20 hover:bg-white/30 text-white font-bold h-10 w-10 rounded-full transition-colors")
                    i(class="fas fa-sign-out-alt")

    // Barra de navegación principal
    nav(class="bg-fuchsia-600")
        div(class="container mx-auto px-4")
            // En pantallas pequeñas (móviles) los enlaces ocuparán todo el ancho
            // En pantallas medianas (md) y grandes, se mostrarán en línea
            div(class="flex flex-col md:flex-row md:justify-center md:items-center md:gap-4")
                a(href="/mis-actividades" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors border-b-2 border-fuchsia-500 md:border-none")
                    i(class="fas fa-home w-5 text-center")
                    span(class="font-bold") Inicio
                //- --- ¡AQUÍ VA EL NUEVO ENLACE CONDICIONAL PARA ADMINS! ---
                //- Solo se muestra si hay un usuario logueado Y si su rol es 'Admin'
                if usuario && usuario.role && usuario.role.nombre === 'Admin'
                    a(href="/admin/usuarios" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors border-b-2 border-fuchsia-500 md:border-none")
                        i(class="fas fa-users-cog w-5 text-center") 
                        span(class="font-bold") Gestionar Usuarios
                a(href="/personal/leer" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors border-b-2 border-fuchsia-500 md:border-none")
                    i(class="fas fa-user w-5 text-center")
                    span(class="font-bold") Personal
                a(href="/cliente/leer" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors border-b-2 border-fuchsia-500 md:border-none")
                    i(class="fas fa-users w-5 text-center")
                    span(class="font-bold") Cliente
                a(href="/proveedor/leer" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors border-b-2 border-fuchsia-500 md:border-none")
                    i(class="fas fa-handshake w-5 text-center")
                    span(class="font-bold") Proveedores
                a(href="/gastos" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors")
                    i(class="fas fa-file-invoice w-5 text-center")
                    span(class="font-bold") Gastos
                a(href="/articulos/leer" class="flex items-center gap-3 py-3 px-4 text-white hover:bg-fuchsia-700 transition-colors")
                    i(class="fas fa-box-open w-5 text-center")
                    span(class="font-bold") Artículos